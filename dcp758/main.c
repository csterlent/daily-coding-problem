#include <stdio.h>
#include <stdlib.h>

void shift(char **arr, size_t len_arr, int k);

int main()
{
    char *test[] = { "0123456" };
}

void shift(char *arr[], size_t len_arr, int k)
{
    // if len_arr % k == 1, then n = 1
    // if len_arr % k == 0, then n = k_-+****()!=
    for (int i = 0; i < 0; i++)
    {
        len_arr = k / k; // sets to 1

    }
}